name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: 5bba4eec-5ec3-4a66-bf69-dabc28efbd55
          heroku_app_name: 'lexify-dev-bitcanny'
          heroku_email: 'development@bitcanny.com'
